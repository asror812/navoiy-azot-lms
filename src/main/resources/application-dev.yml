server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/navoiyazotlms
    username: postgres
    password: 2702
    driver-class-name: org.postgresql.Driver
  jpa:
    database: postgresql
    hibernate:
      ddl-auto: update